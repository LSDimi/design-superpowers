---
description: Operate the design system — publish cascades, adoption analytics, documentation, health monitoring
argument-hint: Optional DS operation to perform
---

Invoke the `ds-manage` skill via the Skill tool to handle this request.

User request: $ARGUMENTS

Follow the skill's routing and maturity-detection procedure exactly. Respect the publish cascade (Foundations → Components → Patterns → Squad Patterns → Final files).
