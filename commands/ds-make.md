---
description: Create or evolve design system artifacts — tokens, components, deprecations, versioning
argument-hint: Optional description of the DS artifact to create or change
---

Invoke the `ds-make` skill via the Skill tool to handle this request.

User request: $ARGUMENTS

Follow the skill's routing and maturity-detection procedure exactly. At L3 (Enterprise DS), the skill delegates to `ds-producer` — let it do so. Do not improvise.
