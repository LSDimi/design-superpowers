---
description: Visual direction, moodboards, palettes, typography, and layout systems
argument-hint: Optional brief describing the direction you want to explore
---

Invoke the `creative` skill via the Skill tool to handle this request.

User request: $ARGUMENTS

Follow the skill's routing and maturity-detection procedure exactly. Do not improvise — the skill defines sub-agent dispatch, knowledge loading, and output format.
