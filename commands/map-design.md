---
description: Extract design language from any artifact and generate a DESIGN.md
argument-hint: Optional source artifact (Figma link, screenshot, site URL) to map
---

Invoke the `map-design` skill via the Skill tool to handle this request.

User request: $ARGUMENTS

Follow the skill's routing and extraction procedure exactly. Output must conform to the DESIGN.md schema in `skills/shared/knowledge/design-md-schema.md`.
