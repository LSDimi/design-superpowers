# PluginOS Operations Reference

> Auto-generated by `scripts/sync-ops.ts`. Do not edit by hand.

Total operations: 26

| Op                      | Category      | Default scope | Description                                                                                                                                                                                                                           |
| ----------------------- | ------------- | ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `lint_detached`         | lint          | selection     | Find all frames that were once component instances but have been detached. Uses naming heuristics to detect likely detached instances. Defaults to selection; pass scope: 'page' to scan the whole page.                              |
| `lint_naming`           | lint          | selection     | Find layers with default names like 'Frame 1', 'Rectangle 2', 'Group 3' that should be renamed for clarity. Defaults to selection; pass scope: 'page' to scan the whole page.                                                         |
| `lint_styles`           | lint          | selection     | Find layers using local styles instead of library styles, or no style at all. Reports fills, strokes, text styles, and effects that don't reference a shared style. Defaults to selection; pass scope: 'page' to scan the whole page. |
| `check_contrast`        | accessibility | selection     | Check color contrast ratios for all text nodes against their parent backgrounds. Reports WCAG AA and AAA compliance. Defaults to selection; pass scope: 'page' to scan the whole page.                                                |
| `check_touch_targets`   | accessibility | selection     | Find interactive elements (buttons, links, inputs) smaller than 44x44px minimum touch target size (WCAG 2.5.8). Defaults to selection; pass scope: 'page' to scan the whole page.                                                     |
| `analyze_overrides`     | components    | selection     | Analyze all component instances and report which have overrides applied, what fields are overridden, and how many. Defaults to selection; pass scope: 'page' to scan the whole page.                                                  |
| `clone_node`            | components    | page          | Clone a node and optionally reposition the copy.                                                                                                                                                                                      |
| `create_frame`          | components    | page          | Create a new frame on the current page with specified dimensions, position, and optional auto-layout.                                                                                                                                 |
| `find_instances`        | components    | page          | Find all instances of a component by name or component key across the current page.                                                                                                                                                   |
| `export_tokens`         | tokens        | page          | Export all local variables as a structured JSON token map, compatible with Tokens Studio format.                                                                                                                                      |
| `list_variables`        | tokens        | page          | List all local variables and variable collections in the file, grouped by collection.                                                                                                                                                 |
| `audit_spacing`         | layout        | selection     | Audit spacing values (padding, gap, item spacing) across auto-layout frames. Reports non-standard values. Defaults to selection; pass scope: 'page' to scan the whole page.                                                           |
| `move_node`             | layout        | page          | Move one or more nodes to a new position.                                                                                                                                                                                             |
| `resize_node`           | layout        | page          | Resize one or more nodes to new dimensions.                                                                                                                                                                                           |
| `populate_text`         | content       | page          | Fill text nodes with placeholder content (lorem ipsum) or custom text. Can target selection or all text nodes matching a name pattern.                                                                                                |
| `set_text`              | content       | page          | Set the text content of one or more text nodes by ID.                                                                                                                                                                                 |
| `extract_css`           | export        | selection     | Extract CSS-like properties from selected nodes or the current page. Returns layout, typography, colors, and sizing as CSS key-value pairs.                                                                                           |
| `extract_palette`       | colors        | page          | Extract all unique solid fill colors used on the current page, with usage counts and which nodes use them.                                                                                                                            |
| `find_non_style_colors` | colors        | selection     | Find all nodes using hardcoded fill colors that are not linked to a local or library style. Defaults to selection; pass scope: 'page' to scan the whole page.                                                                         |
| `set_fills`             | colors        | page          | Set the fill color of one or more nodes by ID.                                                                                                                                                                                        |
| `audit_text_styles`     | typography    | selection     | Audit all text nodes for style consistency. Reports font family, size, weight, and line height usage with counts. Defaults to selection; pass scope: 'page' to scan the whole page.                                                   |
| `list_fonts`            | typography    | page          | List all fonts used in the file with usage counts.                                                                                                                                                                                    |
| `delete_node`           | cleanup       | page          | Delete one or more nodes by ID.                                                                                                                                                                                                       |
| `remove_hidden`         | cleanup       | page          | Find and optionally remove all hidden (invisible) layers on the current page.                                                                                                                                                         |
| `rename_layers`         | cleanup       | page          | Batch rename layers using find/replace, prefix, suffix, or sequential numbering.                                                                                                                                                      |
| `round_values`          | cleanup       | page          | Round all fractional x, y, width, height values to whole pixels for pixel-perfect designs.                                                                                                                                            |
